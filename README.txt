# Usage

Create a jar using and compile(tests will run too)
...
mvn package

Run main using
...
java -cp target/App-1.0-SNAPSHOT.jar com.mycompany.app.App filepath_for_text_file(ex. home/john/file.txt) 